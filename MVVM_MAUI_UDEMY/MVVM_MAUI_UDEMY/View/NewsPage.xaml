<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVM_MAUI_UDEMY.View.NewsPage"
             xmlns:viewmodel="clr-namespace:MVVM_MAUI_UDEMY.ViewModel"
             x:DataType="viewmodel:NewsViewModel"
             xmlns:model="clr-namespace:MVVM_MAUI_UDEMY.Model"
             Title="NewsPage">
    
    <CollectionView Margin="10" 
                    ItemsSource="{Binding newsCollection}" 
                    SelectedItem="{Binding SelectedNews}" 
                    SelectionChangedCommand="{Binding GoToDetailsCommand}" 
                    SelectionMode="Single">
        <CollectionView.ItemsLayout>
            <LinearItemsLayout Orientation="Vertical" ItemSpacing="20"></LinearItemsLayout>
        </CollectionView.ItemsLayout>
        <CollectionView.ItemTemplate >
            <DataTemplate x:DataType="model:News">
                <Frame Padding="5" BorderColor="LightGray" CornerRadius="0">
                    <VerticalStackLayout>
                        <Image HeightRequest="200" Aspect="AspectFill" Source="{Binding ImageUrl}"></Image>
                        <Label FontSize="Medium" Text="{Binding Title}"></Label>
                    </VerticalStackLayout>
                </Frame>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>